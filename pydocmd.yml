generate:
  - cloudselect.md:
      - cloudselect.cloudselect+
  - selector.md:
      - cloudselect.selector+
  - discovery.md:
      - cloudselect.discovery:
          - cloudselect.discovery.discovery:
              - cloudselect.discovery.aws+
              - cloudselect.discovery.hetzner+
              - cloudselect.discovery.kubernetes+
              - cloudselect.discovery.local+
              - cloudselect.discovery.stub+
  - group.md:
      - cloudselect.group:
        - cloudselect.group.group:
          - cloudselect.group.stub+
          - cloudselect.group.simple+
  - pathfinder.md:
      - cloudselect.pathfinder:
        - cloudselect.pathfinder.pathfinder:
          - cloudselect.pathfinder.stub+
          - cloudselect.pathfinder.bastion+
  - report.md:
      - cloudselect.report:
        - cloudselect.report.report:
          - cloudselect.report.json+
          - cloudselect.report.json_pp+
          - cloudselect.report.stub+
          - cloudselect.report.yaml+

pages:
  - Home: index.md
gens_dir: docs/Reference
preprocessor: pydocmd.restructuredtext.Preprocessor
